/**
 * for while
 *
 * for of, for in => [], {}
 */

/**
 * if, else
 * swich
 *
 * 조건 3개 이상 넘어가면 switch
 * 3개 아래면 if
 * 2개면 삼항연산자
 */

const bool1 = true
const bool2 = false
const bool3 = false

if (bool1) {
} else if (bool2) {
} else if (bool3) {
} else {
}

const type = 'A'

switch (false) {
  case 'A' === 'd': {
    asdjklasjdlk
  }
  case 'B' === 'c': {
    asdjkasjdkl
  }
}

/**
 * 삼항연산자
 * 
 * 
    if (response === 'SUCCESS') {
    result = '성공'
    } else {
    result = '실패'
    }   
 */
const response = 'SUCCESS'

const result = response === 'SUCCESS' ? '성공' : '실패'
